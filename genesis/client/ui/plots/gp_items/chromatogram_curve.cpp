#include "chromatogram_curve.h"
using namespace GenesisMarkup;

ChromatogramCurve::ChromatogramCurve(/*ChromatogramPlot *plot*/)
//  : GPGraph(plot->xAxis, plot->yAxis)
  : GPGraph(0, 0)
//  , Plot(plot)
{
//  if (!plot){
//    Q_ASSERT(true);
//  }

}
